# surface_classification
Just run the python files, they will automatically upload the corresponding config in the same folder.
Both surface_classification and surface_classification_v2 use range profile. V2 prints more statistic parameters (not very useful anyways)
